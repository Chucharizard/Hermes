<UserControl x:Class="Hermes.Views.VerDetalleEmpleadoPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Header compacto y profesional -->
            <Border Background="{DynamicResource BackgroundDark}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="{DynamicResource BackgroundDarkestColor}"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Border Background="{DynamicResource PrimaryColor}" CornerRadius="6" Padding="8" Margin="0,0,12,0">
                        <TextBlock Text="👁" FontSize="18"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Detalle del Empleado"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryColor}"/>
                        <TextBlock Text="Información completa del empleado"
                                   FontSize="10"
                                   Foreground="{DynamicResource TextMutedColor}"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Card de perfil compacta -->
            <Border Background="{DynamicResource PrimaryColor}" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="{DynamicResource BackgroundDarkestColor}"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Avatar compacto -->
                    <Border Grid.Column="0" Width="70" Height="70" CornerRadius="35" Background="{DynamicResource PrimaryDarkColor}" Margin="0,0,15,0">
                        <TextBlock Text="👤"
                                   FontSize="36"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Información del empleado -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Margin="0,0,0,4">
                            <Run Text="{Binding Empleado.NombresEmpleado}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <Run Text=" " />
                            <Run Text="{Binding Empleado.ApellidosEmpleado}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </TextBlock>
                        <TextBlock Text="{Binding Empleado.CiEmpleado, StringFormat='CI: {0}'}"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextPrimaryColor}"
                                   Opacity="0.9"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Sección: Información de Contacto -->
            <Border Background="{DynamicResource BackgroundDark}" BorderBrush="{DynamicResource SuccessColor}" BorderThickness="2" CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" Color="{DynamicResource SuccessColorValue}"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Header de sección mejorado -->
                    <Border Background="{DynamicResource SuccessColor}" CornerRadius="6" Padding="10,8" Margin="0,0,0,20" HorizontalAlignment="Left">
                        <TextBlock Text="📞 INFORMACIÓN DE CONTACTO" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextDarkColor}"/>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Teléfono -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Background="{DynamicResource SuccessColor}" CornerRadius="8" Padding="12" Margin="0,0,15,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource SuccessColorValue}"/>
                                </Border.Effect>
                                <TextBlock Text="📱" FontSize="24"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Teléfono" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,4" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Empleado.TelefonoEmpleado}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextSuccessColor}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Correo -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                            <Border Background="{DynamicResource InfoColor}" CornerRadius="8" Padding="12" Margin="0,0,15,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource InfoColorValue}"/>
                                </Border.Effect>
                                <TextBlock Text="✉" FontSize="24"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Correo Electrónico" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,4" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Empleado.CorreoEmpleado}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource InfoColor}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Sección: Cuenta de Usuario -->
            <Border Background="{DynamicResource BackgroundDark}" BorderBrush="{DynamicResource PrimaryColor}" BorderThickness="2" CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" Color="{DynamicResource PrimaryColorValue}"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Header de sección mejorado -->
                    <Border Background="{DynamicResource PrimaryColor}" CornerRadius="6" Padding="10,8" Margin="0,0,0,20" HorizontalAlignment="Left">
                        <TextBlock Text="🔐 CUENTA DE USUARIO" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryColor}"/>
                    </Border>

                    <!-- Tiene usuario -->
                    <StackPanel Visibility="{Binding TieneUsuario, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Nombre de Usuario -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,20">
                                <Border Background="{DynamicResource PrimaryColor}" CornerRadius="8" Padding="12" Margin="0,0,15,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource PrimaryColorValue}"/>
                                    </Border.Effect>
                                    <TextBlock Text="👤" FontSize="24"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Nombre de Usuario" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,4" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Usuario.NombreUsuario}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Rol -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,20">
                                <Border Background="{DynamicResource WarningColor}" CornerRadius="8" Padding="12" Margin="0,0,15,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource WarningColorValue}"/>
                                    </Border.Effect>
                                    <TextBlock Text="🛡" FontSize="24"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Rol del Sistema" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,4" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Usuario.RolId, StringFormat='Rol ID: {0}'}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource WarningColor}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Estado de Usuario -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                                <Border CornerRadius="8" Padding="12" Margin="0,0,15,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Usuario.EsActivoUsuario}" Value="True">
                                                    <Setter Property="Background" Value="#1ABC9C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource PrimaryColorValue}"/>
                                    </Border.Effect>
                                    <TextBlock Text="⚡" FontSize="24"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Estado de Acceso" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,4" FontWeight="SemiBold"/>
                                    <TextBlock FontSize="16" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Inactivo"/>
                                                <Setter Property="Foreground" Value="#E74C3C"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Usuario.EsActivoUsuario}" Value="True">
                                                        <Setter Property="Text" Value="Activo"/>
                                                        <Setter Property="Foreground" Value="#1ABC9C"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!-- No tiene usuario -->
                    <Border CornerRadius="8" Padding="20" Background="{DynamicResource WarningColor}" Visibility="{Binding TieneUsuario, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="{DynamicResource WarningColorValue}"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="{DynamicResource WarningColor}" CornerRadius="25" Width="50" Height="50" Margin="0,0,20,0">
                                <TextBlock Text="ℹ" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextDarkColor}"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Sin Cuenta de Usuario" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource WarningColor}"/>
                                <TextBlock Text="Este empleado no tiene una cuenta de usuario asignada" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Sección: Estado del Empleado -->
            <Border Background="{DynamicResource BackgroundDark}" BorderBrush="{DynamicResource WarningColor}" BorderThickness="2" CornerRadius="10" Padding="25" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" Color="{DynamicResource WarningColorValue}"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Header de sección mejorado -->
                    <Border Background="{DynamicResource WarningColor}" CornerRadius="6" Padding="10,8" Margin="0,0,0,20" HorizontalAlignment="Left">
                        <TextBlock Text="⚙ ESTADO DEL EMPLEADO" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextDarkColor}"/>
                    </Border>

                    <Border CornerRadius="8" Padding="20">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#3a1f1f"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Empleado.EsActivoEmpleado}" Value="True">
                                        <Setter Property="Background" Value="#1f3a1f"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="{DynamicResource PrimaryColorValue}"/>
                        </Border.Effect>

                        <StackPanel Orientation="Horizontal">
                            <Border CornerRadius="30" Width="60" Height="60" Margin="0,0,20,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E74C3C"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Empleado.EsActivoEmpleado}" Value="True">
                                                <Setter Property="Background" Value="#1ABC9C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.5" Color="{DynamicResource PrimaryColorValue}"/>
                                </Border.Effect>
                                <TextBlock FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="❌"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Empleado.EsActivoEmpleado}" Value="True">
                                                    <Setter Property="Text" Value="✅"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>

                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontSize="20" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Empleado Inactivo"/>
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Empleado.EsActivoEmpleado}" Value="True">
                                                    <Setter Property="Text" Value="Empleado Activo"/>
                                                    <Setter Property="Foreground" Value="#1ABC9C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,5,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Este empleado no está disponible para asignaciones"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Empleado.EsActivoEmpleado}" Value="True">
                                                    <Setter Property="Text" Value="Este empleado está disponible para asignaciones"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
