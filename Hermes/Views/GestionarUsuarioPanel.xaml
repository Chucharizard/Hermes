<UserControl x:Class="Hermes.Views.GestionarUsuarioPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Hermes.Helpers">

    <UserControl.Resources>
        <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Header con icono grande -->
            <Border Background="{DynamicResource BackgroundDarkest}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
                </Border.Effect>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="👤" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Gestionar Usuario"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryColor}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Configura el acceso al sistema del empleado"
                               FontSize="11"
                               Foreground="{DynamicResource TextMutedColor}"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Info del Empleado -->
            <Border Background="{DynamicResource PrimaryColor}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.2" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="👨‍💼 " FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding EmpleadoNombreCompleto}"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                        <TextBlock Text="CI: " FontSize="12" Foreground="{DynamicResource TextPrimaryColor}" Opacity="0.9"/>
                        <TextBlock Text="{Binding Empleado.CiEmpleado}" FontSize="12" Foreground="{DynamicResource TextPrimaryColor}" Opacity="0.9"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Sección: Credenciales -->
            <Border Background="{DynamicResource BackgroundDarkest}" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔐 CREDENCIALES DE ACCESO" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,15"/>

                    <!-- Nombre de Usuario -->
                    <TextBlock Text="👤 Nombre de Usuario:" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Usuario.NombreUsuario, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="0,0,0,12"/>

                    <!-- Contraseña -->
                    <TextBlock Text="🔑 Contraseña:" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Height="35"
                                 FontSize="13"
                                 Padding="10,8"
                                 Margin="0,0,0,12"
                                 PasswordChanged="PasswordBox_PasswordChanged"
                                 Background="{DynamicResource BackgroundDarkest}"
                                 Foreground="{DynamicResource TextPrimaryColor}"
                                 BorderBrush="#30FFFFFF"
                                 BorderThickness="1"/>

                    <!-- Confirmar Contraseña -->
                    <TextBlock Text="🔒 Confirmar Contraseña:" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 Height="35"
                                 FontSize="13"
                                 Padding="10,8"
                                 Margin="0,0,0,0"
                                 PasswordChanged="ConfirmPasswordBox_PasswordChanged"
                                 Background="{DynamicResource BackgroundDarkest}"
                                 Foreground="{DynamicResource TextPrimaryColor}"
                                 BorderBrush="#30FFFFFF"
                                 BorderThickness="1"/>
                </StackPanel>
            </Border>

            <!-- Sección: Permisos -->
            <Border Background="{DynamicResource BackgroundDarkest}" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🛡 PERMISOS Y ROL" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource WarningColor}" Margin="0,0,0,15"/>

                    <!-- Rol -->
                    <TextBlock Text="⚡ Rol del Usuario:" FontSize="11" Foreground="{DynamicResource TextMutedColor}" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Roles}"
                              SelectedItem="{Binding RolSeleccionado}"
                              DisplayMemberPath="NombreRol"
                              Style="{StaticResource ModernComboBoxStyle}"
                              Margin="0,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- Sección: Estado -->
            <Border Background="{DynamicResource BackgroundDarkest}" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="⚙ CONFIGURACIÓN" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource WarningColor}" Margin="0,0,0,15"/>

                    <Border Background="{DynamicResource BackgroundDark}" CornerRadius="6" Padding="12">
                        <CheckBox Content="✅ Usuario Activo"
                                  IsChecked="{Binding Usuario.EsActivoUsuario}"
                                  FontSize="12"
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource TextPrimaryColor}"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Mensaje de Error -->
            <Border Background="#3a1f1f"
                    BorderBrush="{DynamicResource DangerColor}"
                    BorderThickness="2"
                    CornerRadius="6"
                    Padding="12"
                    Margin="0,0,0,15"
                    Visibility="{Binding MensajeError, Converter={StaticResource StringToVisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.3" Color="#E74C3C"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" FontSize="16" Foreground="{DynamicResource TextDangerColor}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MensajeError}"
                               Foreground="{DynamicResource TextDangerColor}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Mensaje de Éxito -->
            <Border Background="#1f3a1f"
                    BorderBrush="{DynamicResource SuccessColor}"
                    BorderThickness="2"
                    CornerRadius="6"
                    Padding="12"
                    Margin="0,0,0,15"
                    Visibility="{Binding MensajeExito, Converter={StaticResource StringToVisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.3" Color="#1ABC9C"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✓" FontSize="16" Foreground="{DynamicResource TextSuccessColor}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MensajeExito}"
                               Foreground="{DynamicResource TextSuccessColor}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Botones -->
            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="✖ Cancelar"
                        Command="{Binding DataContext.VolverCommand, RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=2}}"
                        Style="{StaticResource WarningButtonStyle}"
                        Padding="0,12"
                        FontSize="13"
                        FontWeight="SemiBold"/>

                <Button Grid.Column="2"
                        Content="{Binding TextoBotonGuardar}"
                        Command="{Binding GuardarUsuarioCommand}"
                        Background="{DynamicResource PrimaryColor}"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="0,12"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Padding="{TemplateBinding Padding}">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#8E44AD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
