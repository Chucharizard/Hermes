<UserControl x:Class="Hermes.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:Hermes.ViewModels"
             xmlns:converters="clr-namespace:Hermes.Converters">

    <UserControl.DataContext>
        <viewmodels:DashboardViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <converters:PrioridadToColorConverter x:Key="PrioridadToColorConverter"/>
        <converters:EstadoToColorConverter x:Key="EstadoToColorConverter"/>
        <converters:NombreToInitialsConverter x:Key="NombreToInitialsConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header compacto y profesional -->
        <Border Grid.Row="0" Background="#16213e" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Título con icono -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Background="#9B59B6" CornerRadius="8" Padding="10" Margin="0,0,12,0">
                        <TextBlock Text="📊" FontSize="20"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Dashboard"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#FFFFFF"/>
                        <TextBlock Text="Estadísticas generales del sistema"
                                   FontSize="11"
                                   Foreground="#95A5A6"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Botón Dashboard Consolidado -->
                <Button Grid.Column="2"
                        Command="{Binding GenerarDashboardConsolidadoCommand}"
                        IsEnabled="{Binding GenerandoReporte, Converter={StaticResource InverseBooleanConverter}}"
                        Background="#3498DB"
                        Foreground="White"
                        Padding="15,10"
                        FontSize="13"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.3" Color="#3498DB"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2E86AB"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Generar Reporte Excel" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Contenido -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Stats Cards -->
                    <RowDefinition Height="Auto"/> <!-- Compact Widgets: Top 3 Usuarios, Indicadores, Mini Stats -->
                    <RowDefinition Height="Auto"/> <!-- Tareas Urgentes + Actividad -->
                    <RowDefinition Height="Auto"/> <!-- Información del reporte -->
                    <RowDefinition Height="Auto"/> <!-- Botón Actualizar -->
                </Grid.RowDefinitions>

                <!-- Tarjetas de Estadísticas compactas y modernas -->
                <UniformGrid Grid.Row="0" Rows="1" Columns="5" Margin="0,0,0,20">
                    <!-- Total Tareas -->
                    <Border Background="#16213e"
                            BorderBrush="#9B59B6"
                            BorderThickness="2"
                            CornerRadius="10"
                            Margin="0,0,8,0"
                            Padding="18,15"
                            Cursor="Hand"
                            MouseLeftButtonDown="StatsCard_MouseLeftButtonDown"
                            Tag="Total">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a2942"/>
                                        <Setter Property="BorderBrush" Value="#9B59B6"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <Border Background="#9B59B6" CornerRadius="8" Padding="8" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="📋" FontSize="24"/>
                            </Border>
                            <TextBlock Text="{Binding TotalTareas}"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       Foreground="#FFFFFF"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Total Tareas"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#95A5A6"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Pendientes -->
                    <Border Background="#16213e"
                            BorderBrush="#3498DB"
                            BorderThickness="2"
                            CornerRadius="10"
                            Margin="0,0,8,0"
                            Padding="18,15"
                            Cursor="Hand"
                            MouseLeftButtonDown="StatsCard_MouseLeftButtonDown"
                            Tag="Pendiente">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a2942"/>
                                        <Setter Property="BorderBrush" Value="#3498DB"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <Border Background="#3498DB" CornerRadius="8" Padding="8" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="⏳" FontSize="24"/>
                            </Border>
                            <TextBlock Text="{Binding TareasPendientes}"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       Foreground="#FFFFFF"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Pendientes"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#95A5A6"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Completadas -->
                    <Border Background="#16213e"
                            BorderBrush="#1ABC9C"
                            BorderThickness="2"
                            CornerRadius="10"
                            Margin="0,0,8,0"
                            Padding="18,15"
                            Cursor="Hand"
                            MouseLeftButtonDown="StatsCard_MouseLeftButtonDown"
                            Tag="Completado">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a2942"/>
                                        <Setter Property="BorderBrush" Value="#1ABC9C"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <Border Background="#1ABC9C" CornerRadius="8" Padding="8" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="✓" FontSize="24"/>
                            </Border>
                            <TextBlock Text="{Binding TareasCompletadas}"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       Foreground="#FFFFFF"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Completadas"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#95A5A6"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Vencidas -->
                    <Border Background="#16213e"
                            BorderBrush="#E74C3C"
                            BorderThickness="2"
                            CornerRadius="10"
                            Margin="0,0,8,0"
                            Padding="18,15"
                            Cursor="Hand"
                            MouseLeftButtonDown="StatsCard_MouseLeftButtonDown"
                            Tag="Vencido">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a2942"/>
                                        <Setter Property="BorderBrush" Value="#E74C3C"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <Border Background="#E74C3C" CornerRadius="8" Padding="8" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="⚠" FontSize="24"/>
                            </Border>
                            <TextBlock Text="{Binding TareasVencidas}"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       Foreground="#FFFFFF"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Vencidas"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#95A5A6"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Observadas -->
                    <Border Background="#16213e"
                            BorderBrush="#F39C12"
                            BorderThickness="2"
                            CornerRadius="10"
                            Margin="0,0,0,0"
                            Padding="18,15"
                            Cursor="Hand"
                            MouseLeftButtonDown="StatsCard_MouseLeftButtonDown"
                            Tag="Observado">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a2942"/>
                                        <Setter Property="BorderBrush" Value="#F39C12"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.03" Duration="0:0:0.2">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <Border Background="#F39C12" CornerRadius="8" Padding="8" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="👁" FontSize="24"/>
                            </Border>
                            <TextBlock Text="{Binding TareasObservadas}"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       Foreground="#FFFFFF"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Observadas"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#95A5A6"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Widgets Compactos: Top 3 Usuarios, Indicadores de progreso, Mini estadísticas -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Top 3 Usuarios -->
                    <Border Grid.Column="0" Background="#16213e" BorderBrush="#9B59B6" BorderThickness="2" CornerRadius="10" Padding="15" Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Border Background="#9B59B6" CornerRadius="6" Padding="6" Margin="0,0,8,0">
                                    <TextBlock Text="👥" FontSize="14"/>
                                </Border>
                                <TextBlock Text="Top 3 Usuarios" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Usuario 1 -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="10" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="15" Background="#9B59B6" Margin="0,0,10,0">
                                        <TextBlock Text="1" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Top3Usuarios[0].NombreUsuario, FallbackValue='Usuario 1'}" FontSize="12" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tareas completadas" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>

                                    <Border Grid.Column="2" Background="#1ABC9C" CornerRadius="6" Padding="8,4">
                                        <TextBlock Text="{Binding Top3Usuarios[0].TareasCompletadas, FallbackValue='0'}" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Usuario 2 -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="10" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="15" Background="#3498DB" Margin="0,0,10,0">
                                        <TextBlock Text="2" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Top3Usuarios[1].NombreUsuario, FallbackValue='Usuario 2'}" FontSize="12" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tareas completadas" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>

                                    <Border Grid.Column="2" Background="#1ABC9C" CornerRadius="6" Padding="8,4">
                                        <TextBlock Text="{Binding Top3Usuarios[1].TareasCompletadas, FallbackValue='0'}" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Usuario 3 -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="15" Background="#F39C12" Margin="0,0,10,0">
                                        <TextBlock Text="3" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Top3Usuarios[2].NombreUsuario, FallbackValue='Usuario 3'}" FontSize="12" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tareas completadas" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>

                                    <Border Grid.Column="2" Background="#1ABC9C" CornerRadius="6" Padding="8,4">
                                        <TextBlock Text="{Binding Top3Usuarios[2].TareasCompletadas, FallbackValue='0'}" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Indicadores de progreso -->
                    <Border Grid.Column="1" Background="#16213e" BorderBrush="#3498DB" BorderThickness="2" CornerRadius="10" Padding="15" Margin="4,0,4,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Border Background="#3498DB" CornerRadius="6" Padding="6" Margin="0,0,8,0">
                                    <TextBlock Text="📊" FontSize="14"/>
                                </Border>
                                <TextBlock Text="Indicadores de progreso" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Tasa de Completado -->
                            <StackPanel Margin="0,0,0,10">
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Tasa de Completado" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding TasaCompletado, StringFormat={}{0:0}%, FallbackValue='0%'}" FontSize="11" FontWeight="Bold" Foreground="#1ABC9C" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Height="8" Background="#1a1a2e" ClipToBounds="True">
                                    <Rectangle Fill="#1ABC9C" HorizontalAlignment="Left">
                                        <Rectangle.Width>
                                            <MultiBinding>
                                                <MultiBinding.Converter>
                                                    <converters:PercentageWidthConverter/>
                                                </MultiBinding.Converter>
                                                <Binding Path="TasaCompletado" FallbackValue="0"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=Grid}" Path="ActualWidth"/>
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </Grid>
                            </StackPanel>

                            <!-- Tareas a Tiempo -->
                            <StackPanel Margin="0,0,0,10">
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Tareas a Tiempo" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding TasaTiempo, StringFormat={}{0:0}%, FallbackValue='0%'}" FontSize="11" FontWeight="Bold" Foreground="#3498DB" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Height="8" Background="#1a1a2e" ClipToBounds="True">
                                    <Rectangle Fill="#3498DB" HorizontalAlignment="Left">
                                        <Rectangle.Width>
                                            <MultiBinding>
                                                <MultiBinding.Converter>
                                                    <converters:PercentageWidthConverter/>
                                                </MultiBinding.Converter>
                                                <Binding Path="TasaTiempo" FallbackValue="0"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=Grid}" Path="ActualWidth"/>
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </Grid>
                            </StackPanel>

                            <!-- Eficiencia General -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Eficiencia General" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding EficienciaGeneral, StringFormat={}{0:0}%, FallbackValue='0%'}" FontSize="11" FontWeight="Bold" Foreground="#9B59B6" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Height="8" Background="#1a1a2e" ClipToBounds="True">
                                    <Rectangle Fill="#9B59B6" HorizontalAlignment="Left">
                                        <Rectangle.Width>
                                            <MultiBinding>
                                                <MultiBinding.Converter>
                                                    <converters:PercentageWidthConverter/>
                                                </MultiBinding.Converter>
                                                <Binding Path="EficienciaGeneral" FallbackValue="0"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=Grid}" Path="ActualWidth"/>
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Mini estadísticas -->
                    <Border Grid.Column="2" Background="#16213e" BorderBrush="#1ABC9C" BorderThickness="2" CornerRadius="10" Padding="15" Margin="8,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Border Background="#1ABC9C" CornerRadius="6" Padding="6" Margin="0,0,8,0">
                                    <TextBlock Text="📈" FontSize="14"/>
                                </Border>
                                <TextBlock Text="Mini estadísticas" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Tiempo promedio de completado -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#3498DB" CornerRadius="6" Padding="8" Margin="0,0,10,0">
                                        <TextBlock Text="⏱" FontSize="16"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TiempoPromedioCompletado, StringFormat={}{0:0.0} días, FallbackValue='0 días'}" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tiempo promedio" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Tareas por usuario -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#F39C12" CornerRadius="6" Padding="8" Margin="0,0,10,0">
                                        <TextBlock Text="👤" FontSize="16"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TareasPorUsuario, StringFormat={}{0:0.0}, FallbackValue='0'}" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tareas por usuario" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Productividad semanal -->
                            <Border Background="#1a1a2e" CornerRadius="8" Padding="12,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#E74C3C" CornerRadius="6" Padding="8" Margin="0,0,10,0">
                                        <TextBlock Text="📅" FontSize="16"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ProductividadSemanal, StringFormat={}{0:0.0}, FallbackValue='0'}" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF"/>
                                        <TextBlock Text="Tareas/semana" FontSize="9" Foreground="#95A5A6"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Tareas Urgentes y Actividad Reciente -->
                <Grid Grid.Row="2" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tareas Urgentes -->
                    <Border Grid.Column="0" Background="#16213e" BorderBrush="#E74C3C" BorderThickness="2" CornerRadius="10" Padding="15" Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header compacto -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Border Background="#E74C3C" CornerRadius="6" Padding="6" Margin="0,0,8,0">
                                    <TextBlock Text="🔥" FontSize="14"/>
                                </Border>
                                <TextBlock Text="Tareas Urgentes" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                                <Border Background="#E74C3C" CornerRadius="4" Padding="6,2" Margin="8,0,0,0">
                                    <TextBlock Text="{Binding TareasUrgentes.Count}" FontSize="11" FontWeight="Bold" Foreground="#FFFFFF"/>
                                </Border>
                            </StackPanel>

                            <!-- Lista de Tareas Urgentes -->
                            <ItemsControl ItemsSource="{Binding TareasUrgentes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#1a1a2e"
                                                BorderBrush="{Binding PrioridadTarea, Converter={StaticResource PrioridadToColorConverter}}"
                                                BorderThickness="0,0,0,2"
                                                CornerRadius="8"
                                                Padding="10"
                                                Margin="0,0,0,6"
                                                Cursor="Hand">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Título -->
                                                <TextBlock Grid.Row="0"
                                                           Text="{Binding TituloTarea}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           Foreground="#FFFFFF"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <!-- Estado y Prioridad -->
                                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,4">
                                                    <Border Background="{Binding PrioridadTarea, Converter={StaticResource PrioridadToColorConverter}}"
                                                            CornerRadius="4"
                                                            Padding="6,2">
                                                        <TextBlock Text="{Binding PrioridadTarea}"
                                                                   FontSize="9"
                                                                   FontWeight="Bold"
                                                                   Foreground="White"/>
                                                    </Border>
                                                    <Border Background="{Binding EstadoTarea, Converter={StaticResource EstadoToColorConverter}}"
                                                            CornerRadius="4"
                                                            Padding="6,2"
                                                            Margin="4,0,0,0">
                                                        <TextBlock Text="{Binding EstadoTarea}"
                                                                   FontSize="9"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="White"/>
                                                    </Border>
                                                </StackPanel>

                                                <!-- Fecha Límite -->
                                                <StackPanel Grid.Row="2" Orientation="Horizontal">
                                                    <TextBlock Text="⏰" FontSize="10" Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding FechaLimiteTarea, StringFormat=dd/MM/yyyy}"
                                                               FontSize="10"
                                                               Foreground="#E74C3C"
                                                               FontWeight="SemiBold"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Actividad Reciente -->
                    <Border Grid.Column="1" Background="#16213e" BorderBrush="#3498DB" BorderThickness="2" CornerRadius="10" Padding="15" Margin="8,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header compacto -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Border Background="#3498DB" CornerRadius="6" Padding="6" Margin="0,0,8,0">
                                    <TextBlock Text="📌" FontSize="14"/>
                                </Border>
                                <TextBlock Text="Actividad Reciente" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                                <Border Background="#3498DB" CornerRadius="4" Padding="6,2" Margin="8,0,0,0">
                                    <TextBlock Text="{Binding ActividadReciente.Count}" FontSize="11" FontWeight="Bold" Foreground="#FFFFFF"/>
                                </Border>
                            </StackPanel>

                            <!-- Lista de Actividad Reciente -->
                            <ItemsControl ItemsSource="{Binding ActividadReciente}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#1a1a2e"
                                                BorderBrush="#3498DB"
                                                BorderThickness="0,0,0,2"
                                                CornerRadius="8"
                                                Padding="10"
                                                Margin="0,0,0,6"
                                                Cursor="Hand">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Título -->
                                                <TextBlock Grid.Row="0"
                                                           Text="{Binding TituloTarea}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           Foreground="#FFFFFF"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <!-- Estado -->
                                                <Border Grid.Row="1"
                                                        Background="{Binding EstadoTarea, Converter={StaticResource EstadoToColorConverter}}"
                                                        CornerRadius="4"
                                                        Padding="6,2"
                                                        HorizontalAlignment="Left"
                                                        Margin="0,4,0,4">
                                                    <TextBlock Text="{Binding EstadoTarea}"
                                                               FontSize="9"
                                                               FontWeight="SemiBold"
                                                               Foreground="White"/>
                                                </Border>

                                                <!-- Fecha Inicio -->
                                                <StackPanel Grid.Row="2" Orientation="Horizontal">
                                                    <TextBlock Text="📅" FontSize="10" Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding FechaInicioTarea, StringFormat=dd/MM/yyyy HH:mm}"
                                                               FontSize="10"
                                                               Foreground="#95A5A6"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Información del reporte -->
                <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="30" Margin="0,0,0,20">

                    <StackPanel>
                        <TextBlock Text="📊 Dashboard Consolidado Excel"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,15"/>

                        <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="#34495E" Margin="0,0,0,10">
                            <Run Text="El sistema genera un archivo Excel profesional con múltiples hojas de análisis:"/>
                        </TextBlock>

                        <StackPanel Margin="20,10,0,20">
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="📄 " FontWeight="Bold" Foreground="#2E86AB"/>
                                <Run Text="Hoja 1: Dashboard Principal con KPIs visuales"/>
                            </TextBlock>
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="📄 " FontWeight="Bold" Foreground="#A23B72"/>
                                <Run Text="Hoja 2: Datos Detallados y Estadísticas"/>
                            </TextBlock>
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="📄 " FontWeight="Bold" Foreground="#62C370"/>
                                <Run Text="Hoja 3: Análisis y Métricas de Rendimiento"/>
                            </TextBlock>
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="✓ " FontWeight="Bold" Foreground="#27AE60"/>
                                <Run Text="Incluye estadísticas de estados, prioridades y top 3 usuarios"/>
                            </TextBlock>
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="✓ " FontWeight="Bold" Foreground="#3498DB"/>
                                <Run Text="Formatos profesionales con colores y bordes"/>
                            </TextBlock>
                            <TextBlock FontSize="14" Foreground="#34495E" Margin="0,5">
                                <Run Text="✓ " FontWeight="Bold" Foreground="#F39C12"/>
                                <Run Text="Métricas de rendimiento con evaluación automática"/>
                            </TextBlock>
                        </StackPanel>

                        <Border Background="#E8F8F5" Padding="15" CornerRadius="5" BorderBrush="#27AE60" BorderThickness="1">
                            <StackPanel>
                                <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#27AE60" Margin="0,0,0,5">
                                    <Run Text="💡 "/>
                                    <Run Text="Reporte completo y profesional"/>
                                </TextBlock>
                                <TextBlock FontSize="12" Foreground="#2C3E50" TextWrapping="Wrap">
                                    <Run Text="El archivo se guardará en tu Escritorio y se abrirá automáticamente. Incluye todos los reportes en un solo archivo con múltiples hojas organizadas."/>
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Indicador de carga -->
                        <Border Background="#E3F2FD"
                                Padding="15"
                                CornerRadius="5"
                                BorderBrush="#2E86AB"
                                BorderThickness="1"
                                Margin="0,15,0,0"
                                Visibility="{Binding GenerandoReporte, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏳ " FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="Generando dashboard consolidado, por favor espere..."
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="#1565C0"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Botón Actualizar -->
                <Border Grid.Row="4" Style="{StaticResource CardStyle}">
                    <Button Content="🔄 Actualizar Estadísticas"
                            Command="{Binding ActualizarEstadisticasCommand}"
                            Style="{StaticResource InfoButtonStyle}"
                            Padding="30,15"
                            FontSize="16"
                            HorizontalAlignment="Center"/>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
